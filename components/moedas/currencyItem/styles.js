import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  currencyItem: {
    padding: 10,
    borderRadius: 5,
    borderWidth: 1,
    borderColor: 'gray',
    marginBottom: 10,
  },
});

export default styles;
