import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  currencyItem: {
    padding: 12,
    borderRadius: 8,
    borderWidth: 2,
    borderColor: '#000',
    backgroundColor: '#fff',
    marginBottom: 10,
    alignItems: 'center',
  },
});

export default styles;
